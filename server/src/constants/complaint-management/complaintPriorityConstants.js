const COMPLAINT_PRIORITIES = {
    LOW: 'low',
    MEDIUM: 'medium',
    HIGH: 'high',
    URGENT: 'urgent'
};

const PRIORITY_LIST = Object.values(COMPLAINT_PRIORITIES);

module.exports = {
    COMPLAINT_PRIORITIES,
    PRIORITY_LIST
};
